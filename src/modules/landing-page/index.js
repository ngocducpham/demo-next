import React, { Fragment } from 'react';
import Hero from './hero/Hero';

const Landing = ({ news }) => {
    return (
        <Fragment>
            <Hero />
        </Fragment>
    );
};

export default Landing;
